/** Testbank-demo voor scherm 2 (zie src/dev-meermarathon.tsx). Nepdata, geen database. */
export default function Scherm2Demo() {
  return <p className="text-sm text-muted-foreground">Scherm 2: nog geen demo.</p>;
}
