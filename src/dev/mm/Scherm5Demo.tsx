/** Testbank-demo voor scherm 5 (zie src/dev-meermarathon.tsx). Nepdata, geen database. */
export default function Scherm5Demo() {
  return <p className="text-sm text-muted-foreground">Scherm 5: nog geen demo.</p>;
}
