/** Testbank-demo voor scherm 3 (zie src/dev-meermarathon.tsx). Nepdata, geen database. */
export default function Scherm3Demo() {
  return <p className="text-sm text-muted-foreground">Scherm 3: nog geen demo.</p>;
}
