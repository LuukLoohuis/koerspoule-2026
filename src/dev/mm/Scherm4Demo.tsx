/** Testbank-demo voor scherm 4 (zie src/dev-meermarathon.tsx). Nepdata, geen database. */
export default function Scherm4Demo() {
  return <p className="text-sm text-muted-foreground">Scherm 4: nog geen demo.</p>;
}
